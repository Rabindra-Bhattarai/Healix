# Used by docker-compose.yml to bake public client IDs into the frontend
# build (same values as frontend/.env.local's NEXT_PUBLIC_GOOGLE_CLIENT_ID /
# NEXT_PUBLIC_FACEBOOK_APP_ID). Not secret - these are public OAuth client IDs.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
NEXT_PUBLIC_FACEBOOK_APP_ID=
